The task is to set up a repository where we have to built in PM library which is react UI components library consisting of different components like button input cards models etcetera so how we are planning to implement is I have already created a organization named coconut-ui in in PM and I want to publish AUA package where the user can install like @coconut-ui/react.
Under this will be building commands like button input cards etcetera so the user can import components like at @coconut-ui/react/button.
so you are so the task is to set up a repository to build test and publish NPM package like I said above also set up storybook for testing components for previewing everything also keep in mind that the component should be styled using tailwind CSS and the project should be in TypeScript
And another thing to note is that coconut UI is the production organization I have created a test organization coconut beta so we can initially use that organization to publish the components in development and finally a production ready UI is ready we can publish to coconut UI
Now create a detailed implementation plan in the output file specified so that we can review and later when I confirm we can start working on the implementation
OUTPUT FIle: 01.plan.md
